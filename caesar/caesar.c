#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    //get a non-negative integer k
    int k = argv;
    
    if (argc != 2 || isdigit(string argv) = )
    {
        printf ("key: ");
        return 1;
    }
    else
    {
        int k = argv[];
    }

}